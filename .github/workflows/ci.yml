name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        # Simplified matrix with just a single Python version and two OSes
        python-version: ["3.9"]
        os: [ubuntu-latest, macos-latest]
        # Commented out the full matrix for future use
        # python-version: ["3.8", "3.9", "3.10", "3.11"]
        # os: [ubuntu-latest, macos-latest, windows-latest]

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}
        cache: 'pip' # Enable pip caching

    - name: Cache pip packages
      uses: actions/cache@v4
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pytest pytest-cov
        # Install the package itself in editable mode
        pip install -e .

    - name: Install dev tools for linting
      run: |
        python -m pip install --upgrade pip
        pip install ruff==0.8.4

    - name: Lint with ruff
      run: |
        ruff check . --config .ruff.toml

    - name: Test with pytest
      run: |
        pytest tests/ --cov=daolite --cov-report=xml --cov-report=html --junitxml=junit/test-results.xml

    - name: Upload test results
      uses: actions/upload-artifact@v4
      if: always()  # Upload test results even if tests fail
      with:
        name: test-results-${{ matrix.os }}-py${{ matrix.python-version }}
        path: |
          junit/test-results.xml
          coverage.xml
          htmlcov/
